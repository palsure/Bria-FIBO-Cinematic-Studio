# FastAPI and Server (Required)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
mangum>=0.17.0  # ASGI adapter for Vercel serverless functions

# Core Dependencies (Required)
pillow>=10.0.0
numpy>=1.24.0,<2.0.0  # Pin to NumPy 1.x for compatibility

# LLM Translation (Optional - only if using OpenAI/Anthropic)
# openai>=1.0.0
# anthropic>=0.3.0

# Script Parsing (Required)
python-docx>=1.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Export Formats (Optional - only if exporting PDF/Video)
reportlab>=4.0.0  # PDF export
# opencv-python>=4.8.0  # Video export - too large for serverless, disabled
imageio>=2.31.0  # Image I/O
imageio-ffmpeg>=0.4.9  # Video encoding

# Utilities (Required)
pydantic>=2.0.0  # Data validation
python-dotenv>=1.0.0  # Environment variables
requests>=2.31.0  # HTTP client for BRIA API
typing-extensions==4.5.0  # Pin to avoid issubclass() error in Vercel

# Optional: For advanced features (Commented out to reduce size)
# scipy>=1.11.0  # Interpolation
# matplotlib>=3.8.0  # Visualization
# tqdm>=4.66.0  # Progress bars

# Heavy dependencies disabled for serverless (local models not supported)
# torch>=2.0.0  # Too large for serverless
# transformers>=4.30.0  # Too large for serverless
# diffusers>=0.21.0  # Too large for serverless
# huggingface-hub>=0.19.0  # Only needed for local models
# accelerate>=0.25.0  # Only needed for local models

